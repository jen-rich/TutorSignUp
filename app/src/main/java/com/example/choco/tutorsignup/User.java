package com.example.choco.tutorsignup;

public class User {

    private String firstName;
    private String lastName; //also i'm thinking there might be a better way to store this instead of just like this so lmk if you get a better idea
    private boolean accountType; // true -> teacher, false -> student (?) (maybe use an int if we want for additional types (sponsor, officer, etc.))

}
